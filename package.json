{
  "name": "hidden_hand",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ convex-community/hidden_hand",
    "create-local": "graph create --node http://localhost:8020/ convex-community/hidden_hand",
    "remove-local": "graph remove --node http://localhost:8020/ convex-community/hidden_hand",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 convex-community/hidden_hand"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.3",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
